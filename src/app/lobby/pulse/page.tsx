import { TemplatePulseArchive } from '@/templates/discovery/TemplatePulseArchive';

export const metadata = {
  title: 'The Pulse & FM Data | The Lobby — EntireFM',
  description: 'Empirical industry benchmarks and first-party sentiment from UK facilities leaders.',
};

export default function PulsePage() {
  return <TemplatePulseArchive />;
}

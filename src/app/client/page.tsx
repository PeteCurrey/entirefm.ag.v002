/**
 * LEGACY CLIENT PORTAL PAGE — /client
 * =====================================
 * Stub only. Middleware and layout both redirect to canonical /clients.
 * This file prevents 404 on the legacy route during build.
 */
export { default } from '@/app/clients/page';
